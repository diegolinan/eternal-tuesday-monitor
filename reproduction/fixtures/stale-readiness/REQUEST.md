# Evaluation request

Inspect the supplied project files before answering. Determine whether both datasets are currently ready for a costly hardware dispatch. Distinguish remembered summaries from verifiable current artifacts. Return only the structured result requested by the runner.
